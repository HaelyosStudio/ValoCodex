vich_uploader:
    db_driver: orm
    metadata:
        type: attribute

    mappings:
        products:
            uri_prefix: /valo_images
            upload_destination: '%kernel.project_dir%/valo_images'
            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer
